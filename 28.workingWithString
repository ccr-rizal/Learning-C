#include<conio.h>
#include<stdio.h>
#include<string.h>

int main()
{
 
 	char fn[20];
 	char sn[20];
 	char fullname[40];
 	

 		printf("Enter your first name:\n");
 		gets(fn);
 		printf("Enter your first name:\n");
 		gets(sn); 	
		 printf("Her full name is %s \n",  strcat(fn,sn));
		 
		 if(strcmp(fn,sn)==0){
		 	printf("The strings are equal");
		 }else{
		 	printf("The strings are unequal");
		 }
 	return 0;
}
	
